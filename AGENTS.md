# AGENTS.md

This file is optional for Cursor.
Cursor's native mechanism is `.cursor/rules/*.mdc`, so keep the real instructions there.

Use this file only if:
- your team also uses other agents that read `AGENTS.md`
- you want a human-readable summary of the project agent policy

See `.cursor/rules/frontend-agent.mdc` for the actual Cursor rules.
